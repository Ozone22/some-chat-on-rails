<% provide(:title, 'Users') %>
<div class = "row">
  <%= render 'user_sidebar' %>
  <div class="col-md-9">
    <h1>All users</h1>
    <%= will_paginate @users %>
    <ul class="users">
      <%= render @users %>
    </ul>
    <%= will_paginate @users %>
  </div>
</div>